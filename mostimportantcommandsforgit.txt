git init      # git repo creation 
.gitignore    #filetypes to ignore  
git status    # what was modified and being followed
git add -FILENAME    #add file to repository  
git add -A    #add all new files
git commit -m  # commit what what was added to repository  
git push       #  push to remote repository 
               #### when it asks about the credientials of the remore repository try
		#### to add the ssh key instead of the https so later from this local comp
		### you don't need to add password again  

